## 分组密码（简要精练）

### 1) 对加密通信的攻击：选择明文攻击、选择密文攻击

* **选择明文攻击（CPA）**：攻击者可自选明文 (M)，获得对应密文 (C=E_K(M))，目标是区分/恢复信息或密钥。现实场景：能触发系统加密你选的内容。
* **选择密文攻击（CCA）**：攻击者可自选密文 (C)，获得解密结果 (M=D_K(C))（通常不含目标密文），能力强于CPA；典型风险：padding oracle、解密接口被滥用。

> 安全强度：**CCA > CPA**（要求更强）。
>
> KPA（已知明文攻击)

---

### 2) 分组密码的定义

分组密码是一族由密钥 (K) 索引的**等长可逆变换**：
$$
E_K:{0,1}^n \rightarrow {0,1}^n,\quad D_K=E_K^{-1}
$$
理想目标：对未知密钥时，(E_K) 行为近似于**随机置换（PRP）**。

---

### 3) 两种典型构造：Feistel 网络与 S-P 网络

* **Feistel 网络**：每轮只处理半块
  $$
  L_i=R_{i-1},\quad R_i=L_{i-1}\oplus F(R_{i-1},K_i)
  $$
  
  优点：**(F) 不必可逆**；解密可用同结构+反序轮密钥。
* **S-P 网络（SPN）**：多轮“**非线性替换 + 线性扩散 + 加轮密钥**”
  典型轮：SubBytes(S盒) → Perm/ShiftRows → MixColumns(线性扩散) → AddRoundKey。

---

### 4) 常用分组密码：构造方式、密钥长度与安全性

* **DES**：Feistel；密钥 **56-bit**；已可暴力破解，**不安全**。
* **3DES**：基于DES三次；密钥名义168-bit（常见**112-bit安全**）；**安全但很慢**，逐步淘汰。
* **AES**：SPN；密钥 **128/192/256-bit**；当前主流标准，工程上**安全可靠**。
* **SM4**：Feistel（非平衡/变体）；密钥 **128-bit**；工程应用广，通常认为**安全**（无实用破译）。

---

### 5) 分组密码的模式（加密模式）

用于把“固定分组”扩展到“任意长度消息”。

* **ECB**：
  $$
  (C_i=E_K(M_i))
  $$
  不随机，泄露结构，**不安全**。

* **CBC**：
  $$
  (C_i=E_K(M_i\oplus C_{i-1}))
  $$
  需随机IV；加密串行；常与padding绑定。

* **CTR**
  $$
  (C_i=M_i\oplus E_K(\text{nonce}|ctr+i))
  $$
  可并行、高效；**nonce不可复用**。

* **CFB/OFB**：把分组密码当“密钥流生成器”，适合流式场景。

* **AEAD（推荐）**：同时保密+完整性，如 **GCM/CCM**（实际系统优先用）。