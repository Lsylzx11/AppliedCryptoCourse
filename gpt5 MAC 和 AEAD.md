## 1) 完整性（Integrity）及其对安全性的影响

* **完整性**：保证接收方拿到的消息**没有被篡改**（哪怕只改 1 bit 也能被发现）。
* **影响**：主动攻击者可以**篡改/伪造/重放**数据；缺完整性会导致很多严重问题（例如协议被改字段、命令被篡改、甚至利用“错误信息”做攻击如 padding oracle）。
* 因此现代通信通常要求：**机密性 + 完整性 + 认证**（最好一次性由 AEAD 提供）。

---

## 2) MAC（消息认证码）算法的定义

MAC 是一个**带密钥**的认证函数：

* 生成：给定密钥 (K) 和消息 (M)，输出标签 (T)：
  $$
  T = \text{MAC}_K(M)
  $$
  验证：接收方用同一密钥计算并检查 (T) 是否匹配。

**安全目标（核心一句话）**：攻击者在不知道密钥的情况下，无法伪造新的明文标签（M'，T'）对。

---

## 3) AEAD 的定义

AEAD（Authenticated Encryption with Associated Data）= **带关联数据的认证加密**
它把“加密 + 认证”绑定在一起，并支持两类数据：

* **明文 P（要加密且要认证）**
* **关联数据 AAD（不加密但要认证，比如网络包头）**

接口形式：

* 加密：
  $$
  (C,T)=\text{AEAD.Enc}_K(N,\text{AAD},P)
  $$

* 解密：
  $$
  P=\text{AEAD.Dec}_K(N,\text{AAD},C,T)
  $$
  若认证失败则返回 **FAIL**（丢弃密文）。

---

## 4) 常用的 MAC 算法和 AEAD 模式

### 常用 MAC 算法

- **HMAC（如 HMAC-SHA256 / HMAC-SHA1）**：基于**哈希**，通用、易用、软件实现成熟；需要共享密钥。
- **CMAC（如 AES-CMAC / SM4-CMAC）**：基于**分组密码**的 MAC；适合已有 AES/SM4 硬件/指令加速的场景。
- **GMAC**：GCM 的“**只认证**”版本（不加密）；性能好，但强依赖 **nonce/IV 唯一性**。
- **Poly1305**：**一次性**Wegman-Carter MAC；常与 ChaCha20 搭配（ChaCha20-Poly1305），速度快、实现简洁。
- （通信标准里常见）**ZUC-EIA3**：LTE/5G 完整性保护用的 MAC。

CBC-MAC = 用分组密码的 **CBC 模式** 对消息进行加密，但 **只取最后一个分组的密文作为 MAC 标签**。但只能安全用于固定长度消息。

CMAC = 修复了变长不安全问题的 CBC-MAC。

### 常用 AEAD 模式

- **GCM**：最主流；**高性能（**硬件友好）；**nonce/IV 不能重复**。

  AES-GCM / SM4-GCM

- **CCM**：CTR + CBC-MAC；适合受限设备与协议（如部分 IoT/蓝牙场景）；吞吐通常不如 GCM。

  AES-CCM

- **ChaCha20-Poly1305**：移动端/无 AES 硬件时很强；实现抗时序侧信道更容易。

- **OCB**：效率高、nonce不重复，开销小；工程上受许可/生态影响（虽然已有 RFC）。

- （轻量级密码）**ASCON-AEAD**：NIST 轻量级密码标准方向；适合受限环境。

**记忆点**：一般优先 **AEAD**（如 GCM / ChaCha20-Poly1305）；避免 **MAC-then-Encrypt**，常用安全组合是 **Encrypt-then-MAC** 或直接 AEAD。
