### 椭圆曲线

$$
E: y^2 \equiv x^3 + ax + b \pmod p
$$

上定义一种“加法”运算，使得曲线点构成一个**阿贝尔群**。

- 封闭性、交换律、结合律
- 存在单位元（无穷远点 O）:没有任何有限坐标的曲线点能做到“加了等于没加”
- 每个点有逆元：**x 不变，y 取负**

> P=(x,y)，−P=(x,−y mod p)，连接这两个点则直线是 **竖直线**，与曲线的“第三个交点”在无穷远处：即为点O,也就是P+Q=O

#### 加法

1. 无穷远点（单位元）:P+O=O+P=P

2. Q=−P， P+Q=O

3. **P ≠ Q**（普通点加）

   两点确定一条直线，其斜率为：$\lambda = \frac{y_2 - y_1}{x_2 - x_1}=(y2−y1)⋅(x2−x1)^{−1}(modp)$

   除法用乘法逆元代替

4. **P = Q**（倍点）

   退化成曲线在 P 处的切线：斜率

   $\lambda = \frac{dy}{dx} = \frac{3x^2 + a}{2y}= (3x_1^2 + a)\cdot(2y_1)^{-1} \pmod p$

   - 若y1=0，则切线为竖直线，2y1不可逆，交点为无穷远O

根据斜率推交点：
$$
x_3=λ^2−x_1−x_2\\y_3=λ(x_1−x_3)−y_1
$$

#### 标量乘：类快速幂

把一个点在曲线上反复相加 k 次

G 是**已知在曲线上的点**，by**封闭性**：`k·G` 得到的点一定是在曲线上

1. kP=O: P=O 或者 n|k
2. k<0：-k,-P
3. 快速加：右移>> 和 &

| SM2参数 | 含义       | 数学对象 |
| ------- | ---------- | -------- |
| `p`     | 有限域模数 | 𝔽ₚ       |
| `a,b`   | 曲线系数   | 曲线方程 |
| `Gx,Gy` | 基点坐标   | 生成元 G |
| `n`     | G 的阶     | 子群大小 |

本实验采用国家密码管理局推荐的 SM2 椭圆曲线参数 sm2p256。
 曲线定义在有限域 𝔽ₚ 上，其方程为 $y^2 = x^3 + ax + b \pmod p$
 基点$ V=G=(G_x,G_y)$ 的阶为 n，所有密钥生成、加密、签名等操作均在由 G 生成的阶为 n 的子群中完成。

#### ==公钥P=dG==：$d \in [1,n-1]$为私钥

### SM2加密方案

基于椭圆曲线的密钥协商加对称加密方案，通过随机数 k 与接收方公钥生成共享秘密

1.  选取随机数 $k$，计算临时公钥：$C_1 = kG$

   保证**同一明文在不同加密过程中产生不同密文**

2. 利用接收方公钥 $P_k$计算**共享点**：**$(x_2, y_2) = kP_k$**

   这一共享点在加密方和解密方两侧可以独立计算得到，但第三方无法恢复，从而作为双方的共同秘密基础。

3. 将 $x_2||y_2$作为输入，通过 **KDF** 派生与明文等长的对称密钥 $t$（共**享秘钥/秘密）**：$t = sm3_{KDF}(z, len(M)$

4. 明文与密钥异或得到密文主体$C_2 = M \oplus t$

5. 计算摘要 $C_3 = \mathrm{SM3}(x_2 || M || y_2)$用于完整性校验。

6. 密文输出为 $C = C_1 || C_2 || C_3$

椭圆曲线密钥协商 + 对称加密 + 哈希校验

由于椭圆曲线运算成本较高，SM2 并不直接用该共享点对明文进行加密，而是通过密钥派生函数（KDF）将共享点扩展为与明文等长的伪随机密钥流，再通过异或方式对明文进行对称加密。

为防止密文在传输过程中被篡改，引入 SM3 哈希算法，对共享秘密与明文进行摘要计算，生成完整性校验值。解密端通过重新计算该摘要并进行比对，可以判断密文是否被篡改，从而保证数据完整性和正确性。

### SM2解密方案

1. 从密文中解析得到 $C_1、C_2、C_3$，并将私钥字节转换为整数私钥 $sk=d$。
2. 利用私钥计算共享点 $(x_2, y_2) = d \cdot C_1$
3. 将 $x_2||y_2$输入 KDF，生成与 $C_2$等长的密钥流 $t$。
4. 对密文主体执行异或运算 $M = C_2 \oplus t$ 以恢复明文。
5. 计算并校验 $\mathrm{SM3}(x_2||M||y_2)$是否等于 $C_3$，通过则输出明文。

$$
[d]C_1 = d(kG) = k(dG) = kP_k
$$

双方通过不同路径得到**同一个共享点** $(x_2, y_2)$

### SM2签名方案

1. **计算临时点**：计算 `(x1, y1) = kG`
2. **计算 r**：`r = (e + x1) mod n`
3. **计算 s**:`s = (1 + d)⁻¹ · (k − r·d) mod n`
4. **输出签名**: `r || s`（各 32 字节）

### SM2验证方案

1. **计算**：`t = (r + s) mod n`
2. **计算椭圆曲线点**：`(x1, y1) = sG + tP`
3. **计算验证值**：`R = (e + x1) mod n`
4. **判断**：若 `R == r` → **签名有效**，否则 → **无效**

==求逆元==by扩展欧几里得

```cpp
r0, r1 = m, a
x0, x1 = 0, 1

while r1 != 0:
    q = r0 // r1
    r0, r1 = r1, r0 - q*r1
    x0, x1 = x1, x0 - q*x1

# r0 = gcd(a, m)
# x0 * a + y0 * m = r0
return x0 % m
```

### SM2 公钥恢复



**已知**：签名 `(r, s)`，消息摘要 `e`，曲线参数 `(p, a, b, G, n)`

1. **计算**   $t = (r + s) \bmod n$
2. **由签名关系得到 kG 的横坐标**  $x_R = (r - e) \bmod n$
3. **在椭圆曲线上求点 R**
   - 解方程：$y^2 \equiv x_R^3 + a x_R + b \pmod p$:sqrt(公式，p)
   - 得到 0 或 2 个候选点：$R = (x_R, \pm y_R)$
4. **根据验签等式恢复公钥**
   - 利用：$sG + (r+s)P = R$
   - 解得：$P = (r+s)^{-1} \bigl(R - sG\bigr)$
5. **验证并筛选**
   - 对每个候选 `P` 执行一次签名验证
   - 验证通过的即为合法公钥
6. **去重并输出**
   - 返回所有不同的合法公钥（通常 1 个，最多 2 个）

## 

> **先由 `(r,e)` 还原 `kG` 的 x 坐标，再利用验签公式反解 `P`，最后用验签排除假解。**