# 二、密码杂凑函数

## 1. 密码杂凑函数的定义与安全性

### 1.1 定义

**密码杂凑函数（Cryptographic Hash Function）** 是一种函数
$$
H:\{0,1\}^* \rightarrow \{0,1\}^h
$$
它将**任意长度**的输入消息映射为**固定长度**的杂凑值（hash value / message digest），并满足特定的密码学安全性质。

------

### 1.2 安全性要求

**（1）抗原像攻击（Pre-image Resistance）**
 给定一个杂凑值
$$
h = H(M)
$$
在计算上不可行找到对应的原始消息 M。

👉 表明杂凑函数是**单向函数**。

------

**（2）抗第二原像攻击（Second Pre-image Resistance）**
给定一条消息 M，在计算上不可行找到另一条不同的消息 M′≠M，使
$$
H(M) = H(M')
$$
👉 防止攻击者在已知消息的情况下构造等价替代消息。

------

**（3）抗碰撞（Collision Resistance）**
 在计算上不可行找到任意两条不同消息 M1≠M2，使得
$$
H(M_1) = H(M_2)
$$
👉 这是数字签名、证书系统和完整性验证中最关键的性质。

------

## 2. 密码杂凑函数的比特安全性及原理

### 2.1 比特安全性的含义

密码杂凑函数的**安全性主要由输出杂凑值的比特长度决定**。
 假设杂凑函数不存在结构性缺陷，其安全性可由**穷举攻击复杂度**衡量。

------

### 2.2 原理：生日悖论

对于输出长度为 **m 比特** 的杂凑函数：

- **原像攻击复杂度**：约为
  $$
  2^m
  $$

- **第二原像攻击复杂度**：约为
  $$
  2^m
  $$

- **碰撞攻击复杂度**：约为
  $$
  2^{m/2}
  $$

这是由**生日悖论（Birthday Paradox）**决定的。

------

### 2.3 常见杂凑函数的比特安全性示例

| 杂凑函数 | 输出长度 | 碰撞安全性 |
| -------- | -------- | ---------- |
| MD5      | 128 bit  | 64 bit     |
| SHA-1    | 160 bit  | 80 bit     |
| SHA-256  | 256 bit  | 128 bit    |
| SHA-512  | 512 bit  | 256 bit    |

讲义中指出：

- 至少需要 **80 bit** 的安全性
- 现代系统通常要求 **≥128 bit** 碰撞安全性

------

## 3. 常用密码杂凑函数及其构造方式、杂凑值长度和安全性

### 3.1 MD5

- **构造方式**：Merkle–Damgård 构造
- **杂凑值长度**：128 bit
- **安全性**：
  - 存在实用碰撞攻击
  - 已不满足抗碰撞要求（不安全）

------

### 3.2 SHA-1

- **构造方式**：Merkle–Damgård 构造
- **杂凑值长度**：160 bit
- **安全性**：
  - 存在实用化碰撞攻击
  - 已被标准淘汰

------

### 3.3 SHA-2 系列（SHA-256 / SHA-512）

- **构造方式**：Merkle–Damgård 构造
- **杂凑值长度**：
  - SHA-256：256 bit
  - SHA-512：512 bit
- **安全性**：
  - 目前无实用碰撞攻击
  - 广泛使用的安全标准

------

### 3.4 SHA-3（Keccak）

- **构造方式**：Sponge（海绵构造）
- **杂凑值长度**：224 / 256 / 384 / 512 bit
- **安全性**：
  - 抗长度扩展攻击
  - 安全性由容量（capacity）决定
  - 作为 SHA-2 的备选与补充标准

------

### 3.5 其他算法（讲义涉及）

- **RIPEMD-160**：160 bit，应用于比特币地址
- **SM3**：256 bit，中国国家密码标准

------

## 4. 密码杂凑函数的应用

### 4.1 数据完整性与校验

- 文件校验（checksum）
- 软件分发完整性验证

------

### 4.2 身份认证与口令保护

- 服务器端存储口令杂凑值
- 哈希链与一次性口令（OTP）

⚠️ 讲义强调：**直接使用杂凑函数存储口令是不安全的**，需配合 salt 和 KDF。

------

### 4.3 数字签名与公钥基础设施

- 对消息生成固定长度摘要
- 签名对象是摘要而非原文
- 抗碰撞性是核心安全要求

------

### 4.4 分布式系统与区块链

- 分布式哈希表（DHT）
- 区块链中的区块链式哈希结构

------

### 4.5 其他密码学应用

- 密钥派生函数（KDF）
- 消息认证码（MAC）
- 伪随机数生成器（PRNG