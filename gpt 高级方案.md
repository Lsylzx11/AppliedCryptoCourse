根据你给的项目讲义（HE / PIR&OT / Commitment / MPC）内容，简要回答如下：

## 1) 同态加密（HE）的基本概念

同态加密的核心是：把**明文空间上的运算**“搬到”**密文空间**去做——对密文做运算后再解密，得到的结果等价于直接在明文上运算的结果（Enc/Dec 与运算可交换的直观关系）。

## 2) 典型同态加密算法的密文计算能力（明文类型 × 支持运算/数量）

讲义里给了“支持的计算类型”和“能力分层”的框架：

* **支持的运算类型**：算术类（加/减/乘）与逻辑类（XOR/AND/OR）。
* **能力分层（计算数量/深度）**：

  * **PHE（部分同态）**：只支持一种运算（如“加法无限次”或“乘法有限次/一次”等），图里列了典型方案如 RSA、ElGamal、Paillier 等在“+ / × 能力”上的位置。
  * **SHE（某种程度同态）/ Leveled HE**：可支持**有限深度**的加乘电路（乘法会显著增加噪声/代价）。
  * **FHE（全同态）**：理论上支持任意深度电路（通常要靠 bootstrapping 把“噪声”压回去）。
* **以 BFV 这类格基 HE 为例（讲义展开了“为什么乘法更难”）**：

  * EvalAdd 基本是密文分量逐项相加，噪声大致“加性增长”。
  * EvalMult 会让密文结构从 2 个环元素膨胀到 3 个，需要 **relinearization** 把密文“压回去”，并且噪声增长更快。

> 明文类型（整数/比特/近似实数等）在能力图里也有提示（如 floating point 场景对应 CKKS 一类）。

## 3) 隐私协议基本原理（PIR、OT、承诺、秘密共享、混淆电路、零知识等）

* **PIR（私有信息检索）**：客户端能从远程数据库读取某个文档，但**不暴露读取的是哪一条**给服务器。
* **OT（不经意传输）**：类似 PIR，但额外要求客户端**只能学到自己选择的那条消息**；在 1-out-of-2 OT 中，发送方有$m_0,m_1$，接收方有选择位 $b$，协议后接收方得到 $m_b$，但不了解$m_{1-b}$，发送方也不知道 $b$。
* **承诺协议（Commitment）**：先“承诺”后“打开”，满足

  * **Hiding**：打开前接收方学不到值；
  * **Binding**：承诺后发送方不能再改值。
    讲义还给了用承诺做公平掷币（先承诺再揭示再 XOR）的典型用法。
* **秘密共享（Secret Sharing）**：把秘密分成份额分给多人；典型的门限性质是：任意达到阈值的子集可重构，少于阈值无法获得秘密。
* **混淆电路 / 姚式混淆电路（Garbled Circuit）**：一方把布尔电路“混淆”成 garbled tables；输入线的密钥通常通过 OT 让求值方只拿到与其输入对应的那把钥匙，从而能算出输出但学不到对方输入。
* **零知识证明（ZKP）**：但在系统设计里通常用来“证明某个断言成立而不泄露见证/隐私输入”。

## 4) 设计密码应用方案时如何灵活运用这些高级方案（组合思路）

可以把这些“高级积木”按目标组合：

* **“只想把数据交给云算，但不想云看到数据”**：用 HE 把计算搬到密文域（适合统计/模型推理等），但要考虑电路深度与乘法成本（噪声、relinearization 等）。
* **“只想查库但不想暴露查了什么”**：用 PIR；如果还要限制客户端只能拿到“所选那条”，就用 OT 的语义更贴切。
* **“双方都不信任但要公平/防抵赖”**：用承诺做先后顺序与公平性（如公平掷币、先出拳后揭示）。
* **“多方共同计算但各自输入保密”**：用 MPC（混淆电路+OT）做通用安全计算；或用秘密共享把数据拆开、分布式处理，再在阈值下恢复结果。

如果你给我“项目文件”里具体要做的业务目标（例如：隐私检索/联合统计/拍卖/投票/链上证明等），我可以按**威胁模型 + 性能约束（通信/计算/交互轮数）**把上面这些积木组合成一套更贴近落地的方案草图。