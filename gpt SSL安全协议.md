## 1) SSL/TLS 的功能与应用场景

* **功能**：在 TCP 之上提供一条“安全通道”，核心三件事：

  1. **身份认证**（主要认证服务器，可选认证客户端）
  2. **机密性**（会话密钥下的对称加密）
  3. **完整性/抗篡改**（MAC 或 AEAD）
     另外还提供**协商算法**、**防重放/乱序检测（记录序号）**、**会话恢复**等能力。
* **应用场景**：HTTPS(443) 最典型；以及 IMAPS(993)、POP3S(995)、LDAPS(636)、FTPS(990/989) 等“应用层协议跑在 TLS 之上”的安全通信；也可用 **STARTTLS** 在同一端口“升级”到 TLS。

---

## 2) SSL/TLS 握手协议与不同密码套件的安全区别

### 握手做什么（抓住考点）

* **协商参数**：版本、cipher suite、扩展（曲线/签名算法等）
* **认证**：服务器用证书链证明身份；关键参数会被签名绑定到本次握手，防 MITM。
* **密钥建立**：产出 premaster secret → PRF 得到 master_secret → 再导出 record 层用的密钥材料。

### Full Handshake 主流程（会默写）

ClientHello → ServerHello/Certificate/ServerKeyExchange/(CertReq)/ServerHelloDone →
(客户端证书)/ClientKeyExchange/(CertVerify)/ChangeCipherSpec/Finished →
ChangeCipherSpec/Finished 

### 密码套件安全差异（最常考的“比较”）

* **密钥交换方式决定前向安全（FS）**：

  * **(EC)DHE**：临时 DH，泄露长期私钥也**不能解密历史会话**（有前向安全）
  * **RSA 密钥传输**：客户端用服务器 RSA 公钥加密 PMS，**不具备前向安全**（服务器私钥泄露可回溯解密）。
* **记录层保护方式**：

  * **CBC + MAC-then-encrypt** 历史上问题多；
  * **AEAD（如 GCM/CCM）**把加密与认证一体化，现代 TLS 更推荐。
* **认证方式**：RSA/ECDSA/SM2 等签名算法影响兼容性与性能，但“抗 MITM 的关键”是：**握手参数被证书私钥签名并与随机数绑定**。

---

## 3) 国密 SSL（TLCP / GM/T 0024）要点

* 国密体系下的“SSL/TLS 类协议”是 **TLCP（GM/T 0024）**，目标是在握手与记录层中使用国密算法族。
* **双证书机制**（常考特征）：通常区分

  * **签名证书**（用于握手认证/签名）
  * **加密证书**（用于密钥协商/加密相关）
    并包含相应的 CA/中间证书链。
* **算法对应**：常见用 **SM2** 做身份认证/密钥交换相关，**SM4** 做对称加密，**SM3** 做哈希/MAC（具体取决于套件）。

如果你要“考试版一句话对比”：**TLS = 证书认证 +（优选 ECDHE）+（优选 AEAD）**；**国密 TLCP = 同样结构，但算法与证书体系换成国密（突出双证书）**。