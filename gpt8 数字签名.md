## 1) 数字签名的基本概念（必背）

* **目的**：对消息 (M) 提供**认证**、**完整性**、**不可否认**，且**公开可验证**。
* **三算法定义**：
  * $ (sk,pk)=KeyGen(1^k)$
  * $\sigma=Sign(sk,M)$
  * $Verify(pk,M,\sigma)\in{0,1}$
* **MAC vs 签名**：MAC 验证者共享密钥→能伪造；签名验证者不掌握私钥→不能伪造，且可转移给第三方验证。

---

## 2) 基于单向函数的数字签名方案

### Lamport 一次性签名（One-Time Signature, OTS）

* 设消息摘要长度为 $n$（通常先做 Hash，把任意长消息压成  $n$ bit）。
* **KeyGen**：随机取$x_{i,0},x_{i,1}$$(i=1..n)$，令
  $y_{i,b}=f(x_{i,b})$。公钥 $pk={y_{i,b}}$，私钥 $sk={x_{i,b}}$。
* **Sign**：对摘要位串 $m_1...m_n$，签名为
  $\sigma=(x_{1,m_1},x_{2,m_2},...,x_{n,m_n})$。
* **Verify**：检查对每个 $i$，是否$f(x_{i,m_i})=y_{i,m_i}$。

**考点**：

* **只能签一次**：签两个不同消息会逐步泄露 $x_{i,0},x_{i,1}$ 两套私钥，最终可伪造。
* 工程上用 **Hash-then-Sign**：先签 $H(M)$（缩短 $n$，也便于安全论证）。

---

## 3) ElGamal 签名及变体（会写流程 + 关键坑）

### ElGamal 签名（基于 DLP）

* 公参：素数 $p$，生成元 $g$（阶 | (p-1) 的大素因子）。
* **KeyGen**：私钥 $d\in[1,p-2]$，公钥 $y=g^d \bmod p$。
* **Sign**：对 $h=H(M)$
  1. 选随机 $k\in[1,p-2]$，要求$\gcd(k,p-1)=1$
  2. $r=g^k \bmod p$
  3. $s=k^{-1}(h-dr)\bmod (p-1)$
     输出 $(r,s)$
* **Verify**：验 $g^{h}\equiv y^{r}, r^{s}\pmod p$。

**关键考点/坑**：

* **k 保密且每次唯一**：复用/泄露 $k$ 会推出私钥 $d$（和 ECDSA/DSA 同类致命点）。
* **签名较长**：$(r,s)$各约 $\log_2 p$ 位，总长约 $2\log_2 p$。

### 变体

* **Schnorr 签名**：把等式结构改成挑战-响应（常写成$e=H(M|r)$, $s=k-xe)$，签名更紧凑、证明更干净，广泛用于协议/区块链。
* **DSA/ECDSA**：在有限域/椭圆曲线群上实现的 ElGamal 思想变体；ECDSA 用 ECC 提升同等安全下的效率与密钥更短。

---

## 4) 数字签名的安全性和应用

### 安全性

* 目标通常是：在**自适应选择消息攻击**下仍不能伪造新消息的有效签名。
* 依赖因素：

  1. **Hash 抗碰撞**（Hash-then-Sign 防“换文不换签”）
  2. **随机数安全**（DSA/ECDSA/ElGamal 的 (k)）
  3. **私钥保护**（泄露即完蛋）
  4. **验证实现正确**（边界检查、防旁路/实现漏洞）

### 应用

* **签文件/邮件/合同**
* **软件发布与更新**（Windows 内核模块 、Android APK）
* **证书与 PKI**（CA 给公钥证书签名）
* **区块链/支付**（交易签名证明“我授权”）

