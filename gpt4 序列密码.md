## 序列密码

### 1) 一次一密（One-Time Pad）

- **加密**：
  $$
  C = M \oplus K
  $$

- **解密**：
  $$
  M = C \oplus K
  $$
  **安全性**：在密钥 **真随机、等长、只用一次** 时可达**完美保密**（密文不泄露明文信息）。

- **缺点**：密钥管理成本极高（长度=明文长度、还不能复用）。

### 2) 序列密码的概念及其和分组密码的区别

- **概念**：用短密钥 K 驱动 **PRBG/状态机**生成伪随机密钥流 KS，逐比特/逐字节异或：
  $$
  C_i = M_i \oplus KS_i
  $$
  **与分组密码区别（抓重点）**

  - **处理单位**：序列密码“流式逐位/逐字节”；分组密码“固定块（如128-bit）逐块变换”。
  - **随机数/nonce 更关键**：序列密码最怕 **keystream 重用**，通常必须用 nonce/IV/计数器确保每次密钥流不同；分组密码通过工作模式（CBC/CTR/GCM等）处理长消息。
  - **差错传播**：序列密码常见是“翻转一位密文→翻转一位明文”（XOR 结构）；分组模式可能扩散到更多位/块（取决于模式）。

  |          | 序列密码   | 分组密码     |
  | -------- | ---------- | ------------ |
  | 处理单位 | bit / byte | 固定长度分组 |
  | 工作方式 | 流式、在线 | 分块、模式   |
  | 核心风险 | 密钥流复用 | 模式误用     |
  | 错误传播 | 单位级     | 块级         |

  > 一句话：**序列密码 = 密钥流 + XOR；分组密码 = 块置换**

### 3) 序列密码的安全和应用

- **安全核心两条红线**

  1. **密钥流必须不可预测**：普通伪随机（如 `rand()`）可复现/可预测 → 直接破。

  2. **绝不能复用同一密钥流**：若同 keystream 加密两条消息，
     $$
     C_1\oplus C_2=M_1\oplus M_2
     $$
     会泄露大量信息（Office/WEP 都是典型）。

- **算法现状**

  - **RC4**：历史上很常用，但有偏差等弱点 → 已被淘汰/禁用。
  - **现代推荐**：ChaCha20/Salsa20（软件高效）；通信领域有 ZUC（3GPP）。

- **应用场景**：网络传输/实时通信/移动通信等需要**高吞吐、低延迟、易流式处理**的场景；常与认证（MAC/AEAD）组合使用保证完整性与抗篡改。